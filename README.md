# AI Career Navigator

AI Career Navigator — это REST API на Symfony 7.3, использующее Google Gemini AI для помощи в карьерном планировании. Проект предоставляет инструменты для анализа карьеры, улучшения резюме, поиска подходящих профессий и рекомендаций по колледжам в Кыргызстане.

## 🚀 Возможности

- **Карьерный план** — создание персонализированного плана развития карьеры на основе целей и навыков
- **Карьерный тест** — анализ ответов пользователя для определения подходящих профессий
- **Подбор профессий по навыкам** — рекомендация профессий на основе имеющихся навыков
- **Улучшение резюме** — профессиональная оптимизация текста резюме с рекомендациями
- **Поиск колледжей** — рекомендации колледжей и университетов в Кыргызстане для выбранной профессии

## 📋 Требования

- PHP >= 8.2
- Composer
- MySQL/PostgreSQL или другая поддерживаемая СУБД
- Google Gemini API ключ

## 🛠 Установка

### 1. Клонирование репозитория

```bash
git clone git@github.com:Akjol06/AICareerNavigator.git
cd ai-career-navigator
```

### 2. Установка зависимостей

```bash
composer install
```

### 3. Настройка окружения

Создайте файл `.env.local` в корне проекта:

```env
# База данных
DATABASE_URL="mysql://user:password@127.0.0.1:3306/ai_career_navigator?serverVersion=8.0&charset=utf8mb4"
# или для PostgreSQL:
# DATABASE_URL="postgresql://user:password@127.0.0.1:5432/ai_career_navigator?serverVersion=16&charset=utf8"

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# CORS (опционально)
CORS_ALLOW_ORIGIN=http://localhost:3000

# Окружение
APP_ENV=dev
APP_SECRET=your_secret_key_here
```

### 4. Создание базы данных

```bash
php bin/console doctrine:database:create
php bin/console doctrine:migrations:migrate
php bin/console doctrine:fixtures:load
```

### 5. Запуск сервера

```bash
symfony server:start
# или
php -S localhost:8000 -t public
```

API будет доступно по адресу: `http://localhost:8000`

## 📚 API Endpoints

### 1. Поиск колледжей
**POST** `/api/colleges/search`

```json
{
  "profession": "Software Engineer"
}
```

**Ответ:**
```json
{
  "recommendations": "Рекомендации колледжей и университетов..."
}
```

### 2. Создание карьерного плана
**POST** `/api/career-plan`

```json
{
  "goal": "Стать Full Stack разработчиком",
  "skills": ["HTML", "CSS", "JavaScript", "PHP", "Symfony"]
}
```

**Ответ:**
```json
{
  "steps": ["шаг 1", "шаг 2", ...],
  "estimated_time_months": 12,
  "suggested_courses": ["курс 1", "курс 2", ...]
}
```

### 3. Карьерный тест
**POST** `/api/ai/test`

```json
{
  "answers": [
    "Мне нравится работать с цифрами и логикой",
    "Я люблю креативные задачи",
    "Предпочитаю работать в команде",
    "Я хорошо разбираюсь в технологиях"
  ]
}
```

**Ответ:**
```json
{
  "personality_type": "Аналитик",
  "strengths": ["сильная сторона 1", ...],
  "weaknesses": ["слабая сторона 1", ...],
  "suitable_careers": ["профессия 1", ...],
  "skills_to_improve": ["навык 1", ...],
  "summary": "Краткое описание..."
}
```

### 4. Подбор профессий по навыкам
**POST** `/api/skills-match`

```json
{
  "skills": ["Python", "Data Analysis", "Machine Learning"]
}
```

**Ответ:**
```json
{
  "recommendations": "1. Data Scientist\n2. Machine Learning Engineer\n..."
}
```

### 5. Улучшение резюме
**POST** `/api/resume/improve`

```json
{
  "resumeText": "Я разработчик. Занимаюсь PHP и Symfony. Ищу работу."
}
```

**Ответ:**
```json
{
  "improved_text": "Улучшенный текст резюме...",
  "recommendations": [
    "Рекомендация 1",
    "Рекомендация 2",
    "Рекомендация 3"
  ],
  "keywords": [
    "ключевое слово 1",
    "ключевое слово 2"
  ]
}
```

## 📖 Документация API

После запуска сервера документация API Platform доступна по адресу:
- **Swagger UI**: `http://localhost:8000/api/docs`
- **ReDoc**: `http://localhost:8000/api/docs.jsonld`

## 🏗 Структура проекта

```
ai-career-navigator/
├── config/              # Конфигурация Symfony
├── migrations/          # Миграции базы данных
├── public/              # Публичная директория (точка входа)
├── src/
│   ├── Controller/      # Контроллеры API
│   ├── DTO/             # Data Transfer Objects
│   ├── Entity/          # Doctrine сущности
│   ├── Repository/      # Репозитории
│   ├── Service/         # Бизнес-логика
│   │   ├── AI/          # Сервисы для работы с AI
│   │   └── Career/      # Сервисы карьерных функций
│   └── DataFixtures/    # Фикстуры для тестовых данных
└── templates/           # Twig шаблоны
```

## 🔧 Основные компоненты

### Сервисы

- **GeminiService** — интеграция с Google Gemini API
- **CareerPlanService** — создание карьерных планов
- **CareerTestService** — анализ карьерных тестов
- **SkillsMatcherService** — подбор профессий по навыкам
- **ResumeService** — улучшение резюме
- **CollegeSearchService** — поиск колледжей

### Технологии

- **Symfony 7.3** — PHP фреймворк
- **API Platform** — автоматическая генерация REST API
- **Doctrine ORM** — работа с базой данных
- **Google Gemini AI** — искусственный интеллект для анализа
- **Nelmio CORS Bundle** — поддержка CORS

## 🧪 Тестирование

```bash
# Запуск тестов (если настроены)
php bin/phpunit
```

## 📝 Миграции базы данных

```bash
# Создание новой миграции
php bin/console make:migration

# Применение миграций
php bin/console doctrine:migrations:migrate

# Откат последней миграции
php bin/console doctrine:migrations:migrate prev
```

## 🔐 Безопасность

- Убедитесь, что файл `.env.local` не попадает в систему контроля версий
- Используйте сильный `APP_SECRET`
- Настройте CORS для продакшена
- Регулярно обновляйте зависимости

## 🐛 Решение проблем

### Ошибка подключения к базе данных
Проверьте правильность `DATABASE_URL` в `.env.local`

### Ошибка Gemini API
Убедитесь, что `GEMINI_API_KEY` установлен и действителен

### Проблемы с CORS
Настройте `CORS_ALLOW_ORIGIN` в `.env.local`

## 📄 Лицензия

Proprietary

## 👥 Авторы

AI Career Navigator Team

## 🤝 Вклад в проект

Приветствуются pull requests и issues!

